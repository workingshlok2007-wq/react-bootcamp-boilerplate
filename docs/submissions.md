# Submission Tracker

Not generated yet. From the **Actions** tab, run **Generate submission
tracker** (or locally: `GITHUB_TOKEN=<token> node scripts/generate-tracker.mjs`)
once PRs start coming in.

This table tracks which questions have a commit in each student's PR — it's
a completeness check, not a correctness one. Whether the code is actually
right is still a manual/Claude review per PR.
