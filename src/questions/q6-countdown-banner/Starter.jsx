function CountdownBanner() {
  // TODO: add state for seconds remaining (start at 10)
  // TODO: use useEffect + setInterval to tick down every second
  return <p>Sale ends in: 10</p>;
}
export default CountdownBanner;
